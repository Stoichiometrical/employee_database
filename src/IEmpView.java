import java.io.IOException;

public interface IEmpView {
    /**
     * Displays an appropriate message to the user about the class that
     * implements the IEmpView interface and the values of any class variables.
     */

    public abstract void display() throws IOException;
}
